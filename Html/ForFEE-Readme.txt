#For前端开发人员-项目说明

##NameSpace
###公共命名空间前缀->G
###页面CSS私有命名空间
* index.html:in。

##Sass配置文件_config.scss
* 将配置拆分成几个独立的文件分管不同类型的样式，文件具有不同的优先级。
* 凡是在配置文件中的变量，都应带有此文件对应的前缀(推荐单个英文字母，具体请查看相应的配置部分)。
* 页面(局部)变量不得带有任何前缀。