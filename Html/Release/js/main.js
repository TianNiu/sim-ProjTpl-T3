/** 2015,SINOVATE-UED. */
$(document).ready(function() {
    var cur_PG = {
        is_index: $(".in-main").length ? 1 : 0
    };
    /* function description,for page index*/
    if (cur_PG.is_index) {
        console.log("now page is the index");
    }    
});
